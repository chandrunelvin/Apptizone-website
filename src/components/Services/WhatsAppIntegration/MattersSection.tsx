import React from 'react';
import { ServiceMattersSection } from '../shared/ServiceSections';
import { whatsappMarketingConfig } from './serviceData';

const MattersSection: React.FC = () => <ServiceMattersSection config={whatsappMarketingConfig} />;

export default MattersSection;
